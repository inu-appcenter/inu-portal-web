/**
 * 시간표 화면들에서 쓰는 다색 일러스트/로고. 이름으로 조회하지 않는 고정
 * 에셋이라 named export로 둔다.
 *
 * `no-timetable.webp`는 사용자가 이 작업과 별개로 방금 추가해 편집 중이라
 * 이 배럴에 포함하지 않고 `timetable/` 디렉터리에 그대로 두었다.
 */
export { default as EvaluationButtonIcon } from "./evaluation-button.svg?react";
export { default as sugangAppLogo } from "./sugang-app-logo.webp";
export { default as sampleImagePicker } from "./sample-image-picker.webp";
