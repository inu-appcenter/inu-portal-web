/**
 * 학식 점심/저녁 카드 배경 일러스트. data:image base64 래스터 임베드(가짜 SVG)라
 * currentColor 변환이 불가능해 illustrations로 분류했다(감사 보고서 [5]).
 * 소비처(CafeteriaLunch.tsx, CafeteriaDinner.tsx)는 모두 죽은 컴포넌트라
 * 이관만 하고 코드는 그대로 둔다.
 */
export { default as sun } from "./sun.svg";
export { default as sunset } from "./sunset.svg";
