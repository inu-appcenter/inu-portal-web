import editButton from "./edit-button.svg";
import gear from "./gear.svg";

/**
 * 보관함(스크랩) 화면의 편집/설정 아이콘.
 * 대응 글리프 없음 — gear는 톱니, edit-button은 3단 막대라
 * Fontello의 filter(슬라이더)·hamburger-md(균등 3줄)와 모양이 다르다.
 */
export { editButton, gear };
